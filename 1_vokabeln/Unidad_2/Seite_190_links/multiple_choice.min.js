<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Multiple-Choice Quiz: Spanisch Vokabeln</title>
    <style>
        /* Grundlegende Seiten-Einstellungen */
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            margin: 0;
            background-color: #e6f4ea; /* Einheitliches Pastellgrün */
        }

        /* Header Styling */
        header {
            width: 100%;
            background-color: #82c8a0; /* Einheitliches Pastellgrün */
            color: white;
            text-align: center;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        header h1 {
            margin: 0;
            font-size: 1.8em;
        }

        /* Fragebereich und Fortschrittsanzeige */
        #questionContainer {
            width: 400px;
            min-height: 150px;
            background-color: #82c8a0; /* Einheitliches Pastellgrün */
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            font-size: 1.2em;
            text-align: center;
            padding: 20px;
            margin-bottom: 20px;
            color: white;
        }

        #question {
            font-size: 24px;
            color: #2b2b2b;
        }

        /* Timer- und Fortschrittsanzeige */
        #timer, #points, #progress {
            font-size: 18px;
            color: #3c6e47;
            margin-top: 10px;
        }

        #progress-bar {
            width: 80%;
            height: 20px;
            background-color: #cce3d0;
            border-radius: 10px;
            overflow: hidden;
            margin-top: 20px;
        }

        #progress-fill {
            height: 100%;
            background-color: #5e9c76;
            width: 0%;
            transition: width 0.5s ease;
        }

        /* Antwort-Schaltflächen */
        #choices {
            display: flex;
            flex-direction: column;
            gap: 10px;
            width: 100%;
            margin-top: 20px;
        }

        .choiceButton {
            padding: 10px;
            font-size: 16px;
            cursor: pointer;
            border: none;
            border-radius: 5px;
            background-color: #82c8a0; /* Einheitliches Pastellgrün */
            color: white;
            transition: background-color 0.3s;
            width: 100%;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        .choiceButton:hover {
            background-color: #6fae8b;
        }
    </style>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const questions = [
                { question: "'Haus'", choices: ["La casa", "El jardín", "La puerta", "El coche"], correct: "La casa" },
                { question: "'Freund'?", choices: ["El amigo", "La amiga", "El enemigo", "La puerta"], correct: "El amigo" },
                { question: "'Tag'?", choices: ["El día", "La noche", "La casa", "El coche"], correct: "El día" },
            ];

            const totalQuestions = questions.length;
            let currentQuestionIndex = 0;
            let points = 0;

            // Elemente
            const questionContainer = document.getElementById("question");
            const choicesContainer = document.getElementById("choices");
            const pointsDisplay = document.getElementById("points");
            const progressDisplay = document.getElementById("progress");
            const progressBarFill = document.getElementById("progress-fill");

            // Frage laden
            function loadQuestion() {
                const questionData = questions[currentQuestionIndex];
                questionContainer.textContent = questionData.question;
                choicesContainer.innerHTML = "";

                // Sicherstellen, dass die richtige Antwort enthalten ist und zufällige Auswahlmöglichkeiten generieren
                let allChoices = [questionData.correct];
                const availableChoices = questions.flatMap(q => q.choices);
                while (allChoices.length < questionData.choices.length) {
                    const randomChoice = availableChoices[Math.floor(Math.random() * availableChoices.length)];
                    if (!allChoices.includes(randomChoice)) allChoices.push(randomChoice);
                }

                // Auswahlmöglichkeiten mischen
                allChoices = shuffleArray(allChoices);

                // Antwort-Schaltflächen erstellen
                allChoices.forEach(choice => {
                    const button = document.createElement("button");
                    button.classList.add("choiceButton");
                    button.textContent = choice;
                    button.onclick = () => handleAnswer(choice);
                    choicesContainer.appendChild(button);
                });
            }

            // Antwort-Auswahl verarbeiten
            function handleAnswer(selectedChoice) {
                const questionData = questions[currentQuestionIndex];
                if (selectedChoice === questionData.correct) {
                    points++;
                }

                currentQuestionIndex++;

                // Punkte und Fortschritt aktualisieren
                pointsDisplay.textContent = `Punkte: ${points}`;
                updateProgressBar();

                if (currentQuestionIndex < totalQuestions) {
                    loadQuestion();
                } else {
                    questionContainer.textContent = "Quiz abgeschlossen! Gut gemacht!";
                    choicesContainer.innerHTML = "";
                }
            }

            // Fortschrittsbalken aktualisieren
            function updateProgressBar() {
                const progressPercent = (currentQuestionIndex / totalQuestions) * 100;
                progressBarFill.style.width = `${progressPercent}%`;
                progressDisplay.textContent = `Fortschritt: ${currentQuestionIndex} / ${totalQuestions}`;
            }

            // Array mischen
            function shuffleArray(array) {
                for (let i = array.length - 1; i > 0; i--) {
                    const j = Math.floor(Math.random() * (i + 1));
                    [array[i], array[j]] = [array[j], array[i]];
                }
                return array;
            }

            // Quiz initialisieren
            loadQuestion();
            pointsDisplay.textContent = `Punkte: ${points}`;
            updateProgressBar();
        });
    </script>
</head>
<body>
    <header>
        <h1>Multiple-Choice Quiz: Spanisch Vokabeln</h1>
    </header>
    <main>
        <div id="points">Punkte: 0</div>
        <div id="progress">Fortschritt: 0 / 3</div>
        <div id="questionContainer">
            <span id="question">Frage wird geladen...</span>
            <div id="choices"></div>
        </div>
        <div id="progress-bar">
            <div id="progress-fill"></div>
        </div>
    </main>
</body>
</html>
